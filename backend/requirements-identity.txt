# ArcFace identity matching (InsightFace + ONNX Runtime)
# Install after core deps: pip install -r requirements-identity.txt
insightface>=0.7.3
onnxruntime>=1.16.0

# Legacy dlib backend (optional): pip install face-recognition
