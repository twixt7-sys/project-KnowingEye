# Knowing Eye — Backend dependencies (Python 3.10+)
# ----------------------------------------------------------------------
# Core
Django==6.0.3
djangorestframework>=3.15
django-filter>=24.0

# Authentication & Security
djangorestframework-simplejwt>=5.3
python-decouple>=3.8

# Realtime / ASGI
channels>=4.0
daphne>=4.1
# Optional: production-grade Channels backend (set REDIS_URL to enable)
# channels-redis>=4.2

# Database drivers
psycopg2-binary>=2.9

# CORS (frontend communication)
django-cors-headers>=4.3

# Env management
python-dotenv>=1.0

# Image / file handling
Pillow>=10.0

# Computer vision (needed by ai.frame_utils + pipeline)
opencv-python>=4.9
numpy>=1.26

# Production server
gunicorn>=21.2

# Optional: full pipeline_playground ML stack
# Uncomment to enable the production CV pipeline.
# mediapipe>=0.10.14
# ultralytics>=8.3.0
# PyYAML>=6.0.2
