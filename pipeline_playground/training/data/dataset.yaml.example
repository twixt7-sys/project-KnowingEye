# Copy to dataset.yaml and adjust paths after preparing data
# Ultralytics YOLO dataset format

path: training/data  # dataset root (relative to repo root or absolute)
train: images/train
val: images/val

names:
  0: face
  1: bad_posture
  2: cell_phone
